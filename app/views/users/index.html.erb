<% provide(:title, 'All users') %>
<% if current_user.isadmin? && !current_user?(user) %>
<h1>All users</h1>

<ul class="users">
  <%= render @users %>
</ul>
<% else %>
<h1>Insufficient privileges.</h1>
<% end %>